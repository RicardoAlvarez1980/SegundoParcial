document.addEventListener('DOMContentLoaded', function () {
    const main = document.querySelector('main');

    // Aquí puedes escribir la lógica del juego Ahorcado
    // Por ejemplo, puedes crear la interfaz del juego y manejar la interacción del usuario.

    // Ejemplo de cómo agregar contenido al juego (puedes reemplazarlo con tu propia lógica):
    main.innerHTML = `
        <h2>Juego Ahorcado</h2>
        <!-- Aquí puedes agregar la interfaz del juego y la interacción del usuario -->
    `;
});
